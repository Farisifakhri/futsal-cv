# from pathlib import Path

# ROOT = Path(__file__).resolve().parent.parent

# MODEL_PATH = ROOT / "models" / "best_futsal.pt"

# VIDEO_PATH = ROOT / "data" / "clips" / "klip3.mp4"

# OUTPUT_DIR = ROOT / "outputs"

# OUTPUT_DIR.mkdir(exist_ok=True)

# CONFIDENCE = 0.35

# IMAGE_SIZE = 640

# TRACKER_PATH = ROOT / "bytetrack_futsal.yaml"